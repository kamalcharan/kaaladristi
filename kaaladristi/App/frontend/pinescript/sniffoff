// ══════════════════════════════════════════════════════════════════════════════════
// SNIFOFF_51_Lang3 — Multilingual dictionary (Group 3) for Smart Trader, SNIF-OFF 5.1
// Distribution: same policy as SNIFOFF_51_Lang2 (public open-source)
// Scope: Trade Finder rows, Signal Health row, S17/S18/S19 scenarios,
//   PROVISIONAL badge, Native footprint texts, chart-mark tooltips
//   (confluence marks, REV and TREND labels)
// Languages (target): English, Türkçe, العربية, Italiano, हिन्दी, 日本語, 简体中文
// Current build: ALL 7 LANGUAGES — English, Türkçe, العربية, Italiano, हिन्दी,
//   日本語, 简体中文 (Round 8, chart-mark tooltips added). Publish as v2.
// Compiler note (CE10237): exported functions must consume every argument — the
//   `lang` routing structure therefore ships from the first build.
// Architecture: one library holds all 7 languages (no Lang/Lang2-style split).
//   Label functions map English → target and fall through to English (=> en) for
//   unmapped inputs. Key functions return "" for unknown keys — mirrors Lang/Lang2.
// Consumer: ST-EP 5,1 indicator via import ata_sabanci/SNIFOFF_51_Lang3/2 as lang3
//   Replaces every TEMP-EN bypass: S17/S18/S19 label + narrative (desktop + mobile),
//   Trade Finder rows (desktop + mobile Row 5), Signal Health row, PROVISIONAL badge,
//   watch labels, 4 helper affixes, and the inline English chart-mark tooltips
//   (_mTip, _rvTip, _rtTip).
// ══════════════════════════════════════════════════════════════════════════════════

//@version=6

// @description Multilingual translation layer (Group 3) for Smart Trader, SNIF-OFF 5.1, Trade Finder.
//   Provides 12 exported functions covering the Trade Finder and Signal Health dashboard rows,
//   the S17/S18/S19 volume scenarios, the PROVISIONAL badge, the Native footprint texts,
//   the chart-mark tooltips (confluence marks, REV and TREND labels),
//   12 tooltips, and 4 helper affixes.
//   All 7 languages live in this single library; SNIFOFF_51_Lang and SNIFOFF_51_Lang2 stay frozen.
library("SNIFOFF_51_Lang3", overlay = false)

// ══════════════════════════════════════════════════════════════════════════════════
// 1. f_trVol3 — S17/S18/S19 volume scenario labels (3 keys)
// Keys (exact f_volScenarioMsg outputs):
//   "🔥 Pressure Building"  ·  "⚖ High-Volume Standoff"  ·  "◇ Balanced Flow"
// S1–S16 labels stay with Lang/Lang2 — any other input falls through unchanged.
// ══════════════════════════════════════════════════════════════════════════════════

// @function Translates the S17/S18/S19 volume scenario labels.
// @param en The English scenario label from f_volScenarioMsg (S17/S18/S19 keys only).
// @param lang The dashboard language selected by the user.
// @returns The translated scenario label. Unmapped inputs return unchanged.
export f_trVol3(string en, string lang) =>
    if lang == "Türkçe"
        switch en
            "🔥 Pressure Building"    => "🔥 Baskı Birikiyor"
            "⚖ High-Volume Standoff" => "⚖ Yüksek Hacimli Kilitlenme"
            "◇ Balanced Flow"         => "◇ Dengeli Akış"
            => en
    else if lang == "العربية"
        switch en
            "🔥 Pressure Building"    => "🔥 ضغط يتراكم"
            "⚖ High-Volume Standoff" => "⚖ تعادل بحجم مرتفع"
            "◇ Balanced Flow"         => "◇ تدفق متوازن"
            => en
    else if lang == "Italiano"
        switch en
            "🔥 Pressure Building"    => "🔥 Pressione in Aumento"
            "⚖ High-Volume Standoff" => "⚖ Stallo ad Alto Volume"
            "◇ Balanced Flow"         => "◇ Flusso Bilanciato"
            => en
    else if lang == "हिन्दी"
        switch en
            "🔥 Pressure Building"    => "🔥 दबाव बन रहा है"
            "⚖ High-Volume Standoff" => "⚖ उच्च-वॉल्यूम गतिरोध"
            "◇ Balanced Flow"         => "◇ संतुलित प्रवाह"
            => en
    else if lang == "日本語"
        switch en
            "🔥 Pressure Building"    => "🔥 圧力が蓄積中"
            "⚖ High-Volume Standoff" => "⚖ 高出来高の拮抗"
            "◇ Balanced Flow"         => "◇ 均衡フロー"
            => en
    else if lang == "简体中文"
        switch en
            "🔥 Pressure Building"    => "🔥 压力积聚中"
            "⚖ High-Volume Standoff" => "⚖ 高量对峙"
            "◇ Balanced Flow"         => "◇ 均衡流动"
            => en
    else
        en

// ══════════════════════════════════════════════════════════════════════════════════
// 2. f_volNarr3 — S17/S18/S19 volume narratives
// Replaces the inline _s17Narr / _s18Narr / _s19Narr TEMP-EN strings in the indicator.
// English text is byte-identical to the current inline strings (zero visual change).
// ══════════════════════════════════════════════════════════════════════════════════

// @function Builds the S17/S18/S19 volume narrative with optional live-spike overlay.
// @param sid Scenario ID: "S17", "S18", or "S19". Any other ID returns "".
// @param deltaDir Anchored delta direction (+1 buyers, -1 sellers, 0 balanced) — S17 wording.
// @param liveAlert Explosive bar [0] volume spike flag (Live Exception protocol).
// @param lang The dashboard language selected by the user.
// @returns The multi-line narrative string, or "" for unknown scenario IDs.
export f_volNarr3(string sid, int deltaDir, bool liveAlert, string lang) =>
    string _live = ""
    string _body = ""
    if lang == "Türkçe"
        _live := liveAlert ? "\n🔴 CANLI: Mevcut mumda patlayıcı hacim artışı." : ""
        _body := switch sid
            "S17" => "Trendsiz piyasada güçlü hacim (üst çeyrek)\nve net bir " + (deltaDir > 0 ? "alış" : "satış") + " yönlü delta.\nYön netleşmeden katılım artıyor\n— baskı birikiyor.\nHacim daha da yükselirse\nbu bir Kırılım Adayına dönüşür\n— çözülmeyi izleyin."
            "S18" => "Nötr bant içinde dengeli deltayla\nyükselmiş hacim:\nalıcılar ve satıcılar birbirini emiyor.\nBu sessiz bir piyasa değil, yüksek hacimli bir kilitlenme.\nBir taraf pes ettiğinde belirleyici bir hareket bekleyin."
            "S19" => "Son kapanan mumda dengeli alış/satış deltasıyla\naktif bir trend:\nkatılım çift taraflı, net bir inisiyatif yok.\nTrend yapısı sağlam,\nancak bu mum hiçbir yönde hacim güveni eklemiyor.\nDeltanın trendle yeniden hizalanması devamı destekler;\ntrende karşı dönmesi düzeltme uyarısıdır."
            => ""
    else if lang == "العربية"
        _live := liveAlert ? "\n🔴 مباشر: ارتفاع حجم انفجاري على الشمعة الحالية." : ""
        _body := switch sid
            "S17" => "سوق بلا اتجاه مع حجم قوي (الربع الأعلى)\nودلتا واضحة في جانب " + (deltaDir > 0 ? "الشراء" : "البيع") + ".\nالمشاركة ترتفع قبل حسم الاتجاه\n— الضغط يتراكم.\nإذا تصاعد الحجم أكثر\nيتحول هذا إلى مرشح للاختراق\n— راقب لحظة الحسم."
            "S18" => "حجم مرتفع مع دلتا متوازنة\nداخل نطاق الحياد:\nالمشترون والبائعون يمتص كل منهما الآخر.\nهذه ليست سوقاً هادئة بل مواجهة بحجم مرتفع.\nتوقع حركة حاسمة عندما يتراجع أحد الطرفين."
            "S19" => "اتجاه نشط مع دلتا شراء/بيع متوازنة\nعلى آخر شمعة مغلقة:\nالمشاركة من الجانبين بلا مبادرة صافية.\nهيكل الاتجاه لا يزال سليماً،\nلكن هذه الشمعة لا تضيف قناعة حجمية في أي اتجاه.\nعودة الدلتا للتوافق مع الاتجاه ترجّح الاستمرار؛\nوانقلابها ضده تحذير من تصحيح."
            => ""
    else if lang == "Italiano"
        _live := liveAlert ? "\n🔴 LIVE: picco di volume esplosivo sulla candela corrente." : ""
        _body := switch sid
            "S17" => "Mercato senza trend con volume forte (quartile superiore)\ne un delta chiaramente sul lato " + (deltaDir > 0 ? "acquisti" : "vendite") + ".\nLa partecipazione cresce prima che la direzione si risolva\n— la pressione sta aumentando.\nSe il volume sale ulteriormente\nquesto diventa un Candidato al Breakout\n— attendi la risoluzione."
            "S18" => "Volume elevato con delta bilanciato\ndentro la banda di neutralità:\ncompratori e venditori si assorbono a vicenda.\nQuesto è uno stallo ad alto volume, non un mercato calmo.\nAttenditi un movimento decisivo quando un lato cede."
            "S19" => "Un trend attivo con delta acquisti/vendite bilanciato\nsull'ultima candela chiusa:\nla partecipazione è bilaterale, senza iniziativa netta.\nLa struttura del trend resta intatta,\nma questa candela non aggiunge convinzione di volume in nessuna direzione.\nUn delta che si riallinea al trend favorisce la continuazione;\nun'inversione contro di esso avverte di una correzione."
            => ""
    else if lang == "हिन्दी"
        _live := liveAlert ? "\n🔴 लाइव: मौजूदा कैंडल पर विस्फोटक वॉल्यूम उछाल." : ""
        _body := switch sid
            "S17" => "बिना ट्रेंड वाला बाज़ार, मज़बूत वॉल्यूम (शीर्ष चौथाई)\nऔर स्पष्ट " + (deltaDir > 0 ? "खरीद" : "बिक्री") + "-पक्ष डेल्टा.\nदिशा तय होने से पहले भागीदारी बढ़ रही है\n— दबाव बन रहा है.\nयदि वॉल्यूम और बढ़ता है\nतो यह ब्रेकआउट उम्मीदवार बन जाता है\n— समाधान पर नज़र रखें."
            "S18" => "तटस्थता बैंड के भीतर संतुलित डेल्टा के साथ\nऊँचा वॉल्यूम:\nखरीदार और विक्रेता एक-दूसरे को सोख रहे हैं.\nयह शांत बाज़ार नहीं, उच्च-वॉल्यूम गतिरोध है.\nजब एक पक्ष झुकेगा, तब निर्णायक चाल की अपेक्षा करें."
            "S19" => "अंतिम बंद कैंडल पर संतुलित खरीद/बिक्री डेल्टा के साथ\nसक्रिय ट्रेंड:\nभागीदारी दोतरफ़ा है, कोई शुद्ध पहल नहीं.\nट्रेंड संरचना बरकरार है,\nलेकिन यह कैंडल किसी दिशा में वॉल्यूम विश्वास नहीं जोड़ती.\nडेल्टा का ट्रेंड से फिर जुड़ना निरंतरता के पक्ष में है;\nइसके विरुद्ध पलटना सुधार की चेतावनी है."
            => ""
    else if lang == "日本語"
        _live := liveAlert ? "\n🔴 ライブ: 現在のローソク足で爆発的な出来高急増。" : ""
        _body := switch sid
            "S17" => "トレンドのない市場で強い出来高(上位四分位)と\n明確な" + (deltaDir > 0 ? "買い" : "売り") + "側デルタ。\n方向が決まる前に参加が増加中\n— 圧力が蓄積しています。\n出来高がさらに拡大すれば\nブレイクアウト候補となります\n— 決着を見守ってください。"
            "S18" => "中立バンド内の均衡したデルタを伴う\n高い出来高:\n買い手と売り手が互いを吸収しています。\nこれは静かな市場ではなく、高出来高の拮抗です。\n一方が崩れたとき、決定的な動きを想定してください。"
            "S19" => "直近の確定足で買い/売りデルタが均衡した\n活発なトレンド:\n参加は双方向で、純粋な主導権はありません。\nトレンド構造は維持されていますが、\nこの足はどちらの方向にも出来高の確信を加えません。\nデルタがトレンドに再同調すれば継続が優勢;\n逆方向への反転は調整の警告です。"
            => ""
    else if lang == "简体中文"
        _live := liveAlert ? "\n🔴 实时: 当前K线出现爆发性成交量激增。" : ""
        _body := switch sid
            "S17" => "无趋势市场伴随强劲成交量(最高四分位)\n且Delta明显偏向" + (deltaDir > 0 ? "买" : "卖") + "方。\n方向未定之际参与度正在上升\n— 压力正在积聚。\n若成交量进一步放大\n则将成为突破候选\n— 关注最终决出方向。"
            "S18" => "中性区间内Delta均衡\n且成交量偏高:\n买方与卖方正在相互吸收。\n这不是平静的市场，而是高量对峙。\n一旦一方让步，预计将出现决定性行情。"
            "S19" => "最近收盘K线上买卖Delta均衡\n且趋势活跃:\n参与是双向的，没有净主动性。\n趋势结构保持完好，\n但这根K线未在任何方向增加成交量信心。\nDelta重新顺应趋势则利于延续;\n逆势翻转则是回调警告。"
            => ""
    else
        _live := liveAlert ? "\n🔴 LIVE: explosive volume spike on the current candle." : ""
        _body := switch sid
            "S17" => "Trendless market with strong volume (top quartile)\nand a clear " + (deltaDir > 0 ? "buy" : "sell") + "-side delta.\nParticipation is rising before direction is resolved\n— pressure is building.\nIf volume escalates further\nthis becomes a Breakout Candidate\n— watch for the resolution."
            "S18" => "Elevated volume with a balanced delta\ninside the neutrality band:\nbuyers and sellers are absorbing each other.\nThis is a high-volume standoff, not a quiet market.\nExpect a decisive move once one side gives way."
            "S19" => "An active trend with a balanced buy/sell delta\non the last closed bar:\nparticipation is two-sided with no net initiative.\nTrend structure remains intact,\nbut this bar adds no volume conviction in either direction.\nDelta re-engaging with the trend favors continuation;\na flip against it warns of a correction."
            => ""
    _body == "" ? "" : _body + _live

// ══════════════════════════════════════════════════════════════════════════════════
// 3. f_trBadge3 — Signal Health status badge (1 key)
// Key: "◌ PROVISIONAL". ACTIVE / LOCKED / LIVE / HIST badges stay with Lang/Lang2.
// ══════════════════════════════════════════════════════════════════════════════════

// @function Translates the Signal Health PROVISIONAL badge.
// @param en The English badge label ("◌ PROVISIONAL").
// @param lang The dashboard language selected by the user.
// @returns The translated badge label. Unmapped inputs return unchanged.
export f_trBadge3(string en, string lang) =>
    if lang == "Türkçe"
        switch en
            "◌ PROVISIONAL" => "◌ GEÇİCİ"
            => en
    else if lang == "العربية"
        switch en
            "◌ PROVISIONAL" => "◌ مؤقت"
            => en
    else if lang == "Italiano"
        switch en
            "◌ PROVISIONAL" => "◌ PROVVISORIO"
            => en
    else if lang == "हिन्दी"
        switch en
            "◌ PROVISIONAL" => "◌ अस्थायी"
            => en
    else if lang == "日本語"
        switch en
            "◌ PROVISIONAL" => "◌ 暫定"
            => en
    else if lang == "简体中文"
        switch en
            "◌ PROVISIONAL" => "◌ 暂定"
            => en
    else
        en

// ══════════════════════════════════════════════════════════════════════════════════
// 4. f_trTitle3 — Trade Finder + Signal Health section titles (2 keys)
// Keys (exact indicator strings, double spaces around the separator preserved):
//   "TRADE FINDER  ·  Pressure Synthesis"  ·  "SIGNAL HEALTH  ·  Live Monitor"
// Brand policy: TRADE FINDER stays untranslated (part of the product name);
// its subtitle and the full SIGNAL HEALTH title translate normally.
// ══════════════════════════════════════════════════════════════════════════════════

// @function Translates the Trade Finder and Signal Health section titles.
// @param en The English section title.
// @param lang The dashboard language selected by the user.
// @returns The translated section title. Unmapped inputs return unchanged.
export f_trTitle3(string en, string lang) =>
    if lang == "Türkçe"
        switch en
            "TRADE FINDER  ·  Pressure Synthesis" => "TRADE FINDER  ·  Baskı Sentezi"
            "SIGNAL HEALTH  ·  Live Monitor"      => "SİNYAL SAĞLIĞI  ·  Canlı İzleme"
            => en
    else if lang == "العربية"
        switch en
            "TRADE FINDER  ·  Pressure Synthesis" => "TRADE FINDER  ·  توليف الضغط"
            "SIGNAL HEALTH  ·  Live Monitor"      => "صحة الإشارة  ·  مراقبة مباشرة"
            => en
    else if lang == "Italiano"
        switch en
            "TRADE FINDER  ·  Pressure Synthesis" => "TRADE FINDER  ·  Sintesi della Pressione"
            "SIGNAL HEALTH  ·  Live Monitor"      => "SALUTE DEL SEGNALE  ·  Monitor Live"
            => en
    else if lang == "हिन्दी"
        switch en
            "TRADE FINDER  ·  Pressure Synthesis" => "TRADE FINDER  ·  दबाव संश्लेषण"
            "SIGNAL HEALTH  ·  Live Monitor"      => "सिग्नल स्वास्थ्य  ·  लाइव मॉनिटर"
            => en
    else if lang == "日本語"
        switch en
            "TRADE FINDER  ·  Pressure Synthesis" => "TRADE FINDER  ·  圧力統合"
            "SIGNAL HEALTH  ·  Live Monitor"      => "シグナル健全性  ·  ライブモニター"
            => en
    else if lang == "简体中文"
        switch en
            "TRADE FINDER  ·  Pressure Synthesis" => "TRADE FINDER  ·  压力综合"
            "SIGNAL HEALTH  ·  Live Monitor"      => "信号健康  ·  实时监测"
            => en
    else
        en

// ══════════════════════════════════════════════════════════════════════════════════
// 5. f_trTf — Trade Finder + Signal Health state labels (25 keys)
// Desktop states (10): "🐂 Strong Bullish" · "🐻 Strong Bearish" · "▲ Bullish Lean" ·
//   "▼ Bearish Lean" · "◆ Choppy" · "⚠ Mixed Signals" · "💢 Reversal ▲" ·
//   "💢 Reversal ▼" · "💢 Capitulation" · "💢 Exhaustion"
// Mobile compact states (5): "▲ Bull Lean" · "▼ Bear Lean" · "⚠ Mixed" ·
//   "💢 Rev ▲" · "💢 Rev ▼"   (mobile shares the remaining 3 with desktop)
// Watch labels (4): "Trend↔Vol" · "Vol Diverg" · "Counter" · "Eff Fading"
// Signal Health states (6): "✓ Confirming Bullish" · "✓ Confirming Bearish" ·
//   "⚠ Under Attack" · "◆ Neutral" · "◇ Indecision episode" · "— No active episode"
// Non-translatables ("—", "S12", "S13") fall through unchanged by design.
// ══════════════════════════════════════════════════════════════════════════════════

// @function Translates Trade Finder and Signal Health state and watch labels.
// @param en The English state or watch label.
// @param lang The dashboard language selected by the user.
// @returns The translated label. Unmapped inputs return unchanged.
export f_trTf(string en, string lang) =>
    if lang == "Türkçe"
        switch en
            "🐂 Strong Bullish"    => "🐂 Güçlü Boğa"
            "🐻 Strong Bearish"    => "🐻 Güçlü Ayı"
            "▲ Bullish Lean"       => "▲ Boğa Eğilimi"
            "▼ Bearish Lean"       => "▼ Ayı Eğilimi"
            "◆ Choppy"             => "◆ Dalgalı"
            "⚠ Mixed Signals"      => "⚠ Karışık Sinyaller"
            "💢 Reversal ▲"        => "💢 Dönüş ▲"
            "💢 Reversal ▼"        => "💢 Dönüş ▼"
            "💢 Capitulation"      => "💢 Teslimiyet"
            "💢 Exhaustion"        => "💢 Tükenme"
            "▲ Bull Lean"          => "▲ Boğa Eğilimi"
            "▼ Bear Lean"          => "▼ Ayı Eğilimi"
            "⚠ Mixed"              => "⚠ Karışık"
            "💢 Rev ▲"             => "💢 Dönüş ▲"
            "💢 Rev ▼"             => "💢 Dönüş ▼"
            "Trend↔Vol"            => "Trend↔Hacim"
            "Vol Diverg"           => "Hacim Sapması"
            "Counter"              => "Karşıtlık"
            "Eff Fading"           => "Verim Düşüyor"
            "✓ Confirming Bullish" => "✓ Boğa Teyidi"
            "✓ Confirming Bearish" => "✓ Ayı Teyidi"
            "⚠ Under Attack"       => "⚠ Baskı Altında"
            "◆ Neutral"            => "◆ Nötr"
            "◇ Indecision episode" => "◇ Kararsızlık Dönemi"
            "— No active episode"  => "— Aktif Dönem Yok"
            => en
    else if lang == "العربية"
        switch en
            "🐂 Strong Bullish"    => "🐂 صعود قوي"
            "🐻 Strong Bearish"    => "🐻 هبوط قوي"
            "▲ Bullish Lean"       => "▲ ميل صعودي"
            "▼ Bearish Lean"       => "▼ ميل هبوطي"
            "◆ Choppy"             => "◆ متقلب"
            "⚠ Mixed Signals"      => "⚠ إشارات متضاربة"
            "💢 Reversal ▲"        => "💢 انعكاس ▲"
            "💢 Reversal ▼"        => "💢 انعكاس ▼"
            "💢 Capitulation"      => "💢 استسلام"
            "💢 Exhaustion"        => "💢 إرهاق"
            "▲ Bull Lean"          => "▲ ميل صعودي"
            "▼ Bear Lean"          => "▼ ميل هبوطي"
            "⚠ Mixed"              => "⚠ متضارب"
            "💢 Rev ▲"             => "💢 انعكاس ▲"
            "💢 Rev ▼"             => "💢 انعكاس ▼"
            "Trend↔Vol"            => "اتجاه↔حجم"
            "Vol Diverg"           => "تباين الحجم"
            "Counter"              => "معاكسة"
            "Eff Fading"           => "الكفاءة تتراجع"
            "✓ Confirming Bullish" => "✓ تأكيد صعودي"
            "✓ Confirming Bearish" => "✓ تأكيد هبوطي"
            "⚠ Under Attack"       => "⚠ تحت الضغط"
            "◆ Neutral"            => "◆ محايد"
            "◇ Indecision episode" => "◇ فترة تردد"
            "— No active episode"  => "— لا فترة نشطة"
            => en
    else if lang == "Italiano"
        switch en
            "🐂 Strong Bullish"    => "🐂 Fortemente Rialzista"
            "🐻 Strong Bearish"    => "🐻 Fortemente Ribassista"
            "▲ Bullish Lean"       => "▲ Inclinazione Rialzista"
            "▼ Bearish Lean"       => "▼ Inclinazione Ribassista"
            "◆ Choppy"             => "◆ Laterale"
            "⚠ Mixed Signals"      => "⚠ Segnali Misti"
            "💢 Reversal ▲"        => "💢 Inversione ▲"
            "💢 Reversal ▼"        => "💢 Inversione ▼"
            "💢 Capitulation"      => "💢 Capitolazione"
            "💢 Exhaustion"        => "💢 Esaurimento"
            "▲ Bull Lean"          => "▲ Incl. Rialzista"
            "▼ Bear Lean"          => "▼ Incl. Ribassista"
            "⚠ Mixed"              => "⚠ Misti"
            "💢 Rev ▲"             => "💢 Inv. ▲"
            "💢 Rev ▼"             => "💢 Inv. ▼"
            "Trend↔Vol"            => "Trend↔Volume"
            "Vol Diverg"           => "Diverg. Volume"
            "Counter"              => "Contromossa"
            "Eff Fading"           => "Efficienza in Calo"
            "✓ Confirming Bullish" => "✓ Conferma Rialzista"
            "✓ Confirming Bearish" => "✓ Conferma Ribassista"
            "⚠ Under Attack"       => "⚠ Sotto Attacco"
            "◆ Neutral"            => "◆ Neutrale"
            "◇ Indecision episode" => "◇ Episodio di Indecisione"
            "— No active episode"  => "— Nessun episodio attivo"
            => en
    else if lang == "हिन्दी"
        switch en
            "🐂 Strong Bullish"    => "🐂 मज़बूत तेज़ी"
            "🐻 Strong Bearish"    => "🐻 मज़बूत मंदी"
            "▲ Bullish Lean"       => "▲ तेज़ी झुकाव"
            "▼ Bearish Lean"       => "▼ मंदी झुकाव"
            "◆ Choppy"             => "◆ अस्थिर"
            "⚠ Mixed Signals"      => "⚠ मिश्रित संकेत"
            "💢 Reversal ▲"        => "💢 पलटाव ▲"
            "💢 Reversal ▼"        => "💢 पलटाव ▼"
            "💢 Capitulation"      => "💢 समर्पण"
            "💢 Exhaustion"        => "💢 थकावट"
            "▲ Bull Lean"          => "▲ तेज़ी झुकाव"
            "▼ Bear Lean"          => "▼ मंदी झुकाव"
            "⚠ Mixed"              => "⚠ मिश्रित"
            "💢 Rev ▲"             => "💢 पलटाव ▲"
            "💢 Rev ▼"             => "💢 पलटाव ▼"
            "Trend↔Vol"            => "ट्रेंड↔वॉल्यूम"
            "Vol Diverg"           => "वॉल्यूम विचलन"
            "Counter"              => "प्रतिकूल चाल"
            "Eff Fading"           => "दक्षता घट रही"
            "✓ Confirming Bullish" => "✓ तेज़ी की पुष्टि"
            "✓ Confirming Bearish" => "✓ मंदी की पुष्टि"
            "⚠ Under Attack"       => "⚠ दबाव में"
            "◆ Neutral"            => "◆ तटस्थ"
            "◇ Indecision episode" => "◇ अनिर्णय अवधि"
            "— No active episode"  => "— कोई सक्रिय अवधि नहीं"
            => en
    else if lang == "日本語"
        switch en
            "🐂 Strong Bullish"    => "🐂 強い強気"
            "🐻 Strong Bearish"    => "🐻 強い弱気"
            "▲ Bullish Lean"       => "▲ 強気傾向"
            "▼ Bearish Lean"       => "▼ 弱気傾向"
            "◆ Choppy"             => "◆ 方向感なし"
            "⚠ Mixed Signals"      => "⚠ シグナル混在"
            "💢 Reversal ▲"        => "💢 反転 ▲"
            "💢 Reversal ▼"        => "💢 反転 ▼"
            "💢 Capitulation"      => "💢 投げ売り"
            "💢 Exhaustion"        => "💢 買い疲れ"
            "▲ Bull Lean"          => "▲ 強気傾向"
            "▼ Bear Lean"          => "▼ 弱気傾向"
            "⚠ Mixed"              => "⚠ 混在"
            "💢 Rev ▲"             => "💢 反転 ▲"
            "💢 Rev ▼"             => "💢 反転 ▼"
            "Trend↔Vol"            => "トレンド↔出来高"
            "Vol Diverg"           => "出来高乖離"
            "Counter"              => "逆行"
            "Eff Fading"           => "効率低下"
            "✓ Confirming Bullish" => "✓ 強気を確認"
            "✓ Confirming Bearish" => "✓ 弱気を確認"
            "⚠ Under Attack"       => "⚠ 圧迫下"
            "◆ Neutral"            => "◆ 中立"
            "◇ Indecision episode" => "◇ 迷いの局面"
            "— No active episode"  => "— 有効な局面なし"
            => en
    else if lang == "简体中文"
        switch en
            "🐂 Strong Bullish"    => "🐂 强烈看涨"
            "🐻 Strong Bearish"    => "🐻 强烈看跌"
            "▲ Bullish Lean"       => "▲ 偏多"
            "▼ Bearish Lean"       => "▼ 偏空"
            "◆ Choppy"             => "◆ 震荡"
            "⚠ Mixed Signals"      => "⚠ 信号混杂"
            "💢 Reversal ▲"        => "💢 反转 ▲"
            "💢 Reversal ▼"        => "💢 反转 ▼"
            "💢 Capitulation"      => "💢 恐慌抛售"
            "💢 Exhaustion"        => "💢 买盘衰竭"
            "▲ Bull Lean"          => "▲ 偏多"
            "▼ Bear Lean"          => "▼ 偏空"
            "⚠ Mixed"              => "⚠ 混杂"
            "💢 Rev ▲"             => "💢 反转 ▲"
            "💢 Rev ▼"             => "💢 反转 ▼"
            "Trend↔Vol"            => "趋势↔成交量"
            "Vol Diverg"           => "量能背离"
            "Counter"              => "逆向冲击"
            "Eff Fading"           => "效率衰减"
            "✓ Confirming Bullish" => "✓ 确认看涨"
            "✓ Confirming Bearish" => "✓ 确认看跌"
            "⚠ Under Attack"       => "⚠ 受压"
            "◆ Neutral"            => "◆ 中性"
            "◇ Indecision episode" => "◇ 犹豫阶段"
            "— No active episode"  => "— 无活跃阶段"
            => en
    else
        en

// ══════════════════════════════════════════════════════════════════════════════════
// 6. f_tfNarrative — Trade Finder narrative builder
// Replaces the inline _tfNarrTxt TEMP-EN chain plus the S12/S13 override texts.
// State codes 1..8 mirror _tfState; 9 and 10 are the desktop footprint overrides:
//   1 Strong Bullish · 2 Strong Bearish · 3 Bullish Lean · 4 Bearish Lean ·
//   5 Choppy · 6 Mixed Signals · 7 Reversal Watch ▲ · 8 Reversal Watch ▼ ·
//   9 Capitulation (S12 evidence) · 10 Exhaustion (S13 evidence)
// English output is byte-identical to the current inline strings.
// ══════════════════════════════════════════════════════════════════════════════════

// @function Builds the Trade Finder narrative row text.
// @param state State code 1..10 (see section header for the mapping).
// @param dir Composite pressure direction (+1 bullish, -1 bearish, 0 flat).
// @param consensus Number of dimensions agreeing with the composite direction (0-5).
// @param lang The dashboard language selected by the user.
// @returns The narrative string for the Trade Finder dashboard row.
// @param scN Active scale count: 6 in classic mode, 19 in Microscope mode.
export f_tfNarrative(int state, int dir, int consensus, int scN, string lang) =>
    string _cap = str.tostring(int(math.ceil(scN / 2.0)))
    string _n = ""
    if lang == "Türkçe"
        if state == 9
            _n := _cap + "+ ayak izi ölçeğinde satış teslimiyeti.\nDüşüş trendi yapısı tükeniyor olabilir."
        else if state == 10
            _n := _cap + "+ ayak izi ölçeğinde alış tükenmesi.\nYükseliş trendi yapısı tükeniyor olabilir."
        else if state <= 2
            _n := (dir > 0 ? "Boğa" : "Ayı") + " baskısı: " + str.tostring(consensus) + "/5 boyut hemfikir.\nİzleyin: delta dönüşü veya M4 zayıflaması."
        else if state <= 4
            _n := (dir > 0 ? "Boğa" : "Ayı") + " eğilimi\n— henüz tüm boyutlar teyit etmiyor."
        else if state == 5
            _n := "Boyutlar arasında uzlaşı yok."
        else if state == 6
            _n := "Boyutlar çelişiyor\n— trend-hacim çatışması veya karşıtlık/sapma aktif."
        else
            _n := (state == 7 ? "Boğa" : "Ayı") + " CASCADE\n— yapısal bir dönüş oluşuyor olabilir."
    else if lang == "العربية"
        if state == 9
            _n := "استسلام بيعي على " + _cap + "+ من مقاييس البصمة.\nهيكل الاتجاه الهبوطي قد يقترب من الاستنفاد."
        else if state == 10
            _n := "إرهاق شرائي على " + _cap + "+ من مقاييس البصمة.\nهيكل الاتجاه الصعودي قد يقترب من الاستنفاد."
        else if state <= 2
            _n := "ضغط " + (dir > 0 ? "صعودي" : "هبوطي") + ": " + str.tostring(consensus) + "/5 أبعاد متفقة.\nراقب: انقلاب الدلتا أو تراجع M4."
        else if state <= 4
            _n := "ميل " + (dir > 0 ? "صعودي" : "هبوطي") + "\n— ليست كل الأبعاد تؤكد بعد."
        else if state == 5
            _n := "لا إجماع بين الأبعاد."
        else if state == 6
            _n := "الأبعاد متضاربة\n— تعارض اتجاه وحجم أو معاكسة/تباين نشط."
        else
            _n := (state == 7 ? "CASCADE صعودي" : "CASCADE هبوطي") + "\n— انعكاس هيكلي قد يتشكل."
    else if lang == "Italiano"
        if state == 9
            _n := "Capitolazione di vendita su " + _cap + "+ scale del footprint.\nLa struttura del downtrend potrebbe esaurirsi."
        else if state == 10
            _n := "Esaurimento degli acquisti su " + _cap + "+ scale del footprint.\nLa struttura dell'uptrend potrebbe esaurirsi."
        else if state <= 2
            _n := "Pressione " + (dir > 0 ? "rialzista" : "ribassista") + ": " + str.tostring(consensus) + "/5 dimensioni concordi.\nOsserva: inversione del delta o indebolimento M4."
        else if state <= 4
            _n := "Inclinazione " + (dir > 0 ? "rialzista" : "ribassista") + "\n— non tutte le dimensioni confermano ancora."
        else if state == 5
            _n := "Nessun consenso tra le dimensioni."
        else if state == 6
            _n := "Le dimensioni sono in conflitto\n— trend contro volume oppure contromossa/divergenza attiva."
        else
            _n := "CASCADE " + (state == 7 ? "rialzista" : "ribassista") + "\n— potrebbe formarsi un'inversione strutturale."
    else if lang == "हिन्दी"
        if state == 9
            _n := _cap + "+ फुटप्रिंट पैमानों पर बिक्री समर्पण.\nडाउनट्रेंड संरचना थक रही हो सकती है."
        else if state == 10
            _n := _cap + "+ फुटप्रिंट पैमानों पर खरीद थकावट.\nअपट्रेंड संरचना थक रही हो सकती है."
        else if state <= 2
            _n := (dir > 0 ? "तेज़ी" : "मंदी") + " दबाव: " + str.tostring(consensus) + "/5 आयाम सहमत.\nनज़र रखें: डेल्टा पलटाव या M4 कमज़ोरी."
        else if state <= 4
            _n := (dir > 0 ? "तेज़ी" : "मंदी") + " झुकाव\n— अभी सभी आयाम पुष्टि नहीं करते."
        else if state == 5
            _n := "आयामों के बीच कोई सहमति नहीं."
        else if state == 6
            _n := "आयाम टकरा रहे हैं\n— ट्रेंड बनाम वॉल्यूम या प्रतिकूल चाल/विचलन सक्रिय."
        else
            _n := (state == 7 ? "तेज़ी" : "मंदी") + " CASCADE\n— संरचनात्मक पलटाव बन सकता है."
    else if lang == "日本語"
        if state == 9
            _n := _cap + "以上のフットプリント尺度で投げ売り。\n下降トレンド構造は枯渇しつつある可能性。"
        else if state == 10
            _n := _cap + "以上のフットプリント尺度で買い疲れ。\n上昇トレンド構造は枯渇しつつある可能性。"
        else if state <= 2
            _n := (dir > 0 ? "強気" : "弱気") + "圧力: " + str.tostring(consensus) + "/5次元が一致。\n注視: デルタ反転またはM4減衰。"
        else if state <= 4
            _n := (dir > 0 ? "強気" : "弱気") + "傾向\n— まだ全次元が確認していません。"
        else if state == 5
            _n := "次元間のコンセンサスなし。"
        else if state == 6
            _n := "次元が対立\n— トレンド対出来高、または逆行/乖離が有効。"
        else
            _n := (state == 7 ? "強気" : "弱気") + " CASCADE\n— 構造的反転が形成中の可能性。"
    else if lang == "简体中文"
        if state == 9
            _n := _cap + "个以上足迹尺度出现恐慌抛售。\n下跌趋势结构可能正在衰竭。"
        else if state == 10
            _n := _cap + "个以上足迹尺度出现买盘衰竭。\n上涨趋势结构可能正在衰竭。"
        else if state <= 2
            _n := (dir > 0 ? "看涨" : "看跌") + "压力: " + str.tostring(consensus) + "/5个维度一致。\n关注: Delta翻转或M4衰减。"
        else if state <= 4
            _n := (dir > 0 ? "看涨" : "看跌") + "倾向\n— 尚未获得所有维度确认。"
        else if state == 5
            _n := "各维度之间没有共识。"
        else if state == 6
            _n := "维度相互冲突\n— 趋势与成交量相悖，或逆向/背离生效。"
        else
            _n := (state == 7 ? "看涨" : "看跌") + " CASCADE\n— 结构性反转可能正在形成。"
    else
        if state == 9
            _n := "Selling capitulation on " + _cap + "+ footprint scales.\nDowntrend structure may be exhausting."
        else if state == 10
            _n := "Buying exhaustion on " + _cap + "+ footprint scales.\nUptrend structure may be exhausting."
        else if state <= 2
            _n := (dir > 0 ? "Bullish" : "Bearish") + " pressure: " + str.tostring(consensus) + "/5 dimensions agree.\nWatch: delta flip or M4 fade."
        else if state <= 4
            _n := (dir > 0 ? "Bullish" : "Bearish") + " lean\n— not all dimensions confirm yet."
        else if state == 5
            _n := "No consensus across dimensions."
        else if state == 6
            _n := "Dimensions conflict\n— trend vs volume or counter/divergence active."
        else
            _n := (state == 7 ? "Bullish" : "Bearish") + " CASCADE\n— structural reversal may be forming."
    _n

// ══════════════════════════════════════════════════════════════════════════════════
// 7. f_trTipTf — Trade Finder + Signal Health tooltips (12 keys)
// Trade Finder keys (7): tf_title, tf_badge, tf_state, tf_score, tf_cons, tf_watch, tf_narr
// Signal Health keys (5): sh_title, sh_badge, sh_state, sh_score, sh_dom
// English text is byte-identical to the current inline tooltips in the indicator.
// ══════════════════════════════════════════════════════════════════════════════════

// @function Returns translated tooltip text for Trade Finder and Signal Health cells.
// @param key Tooltip key identifier (see section header for valid keys).
// @param lang The dashboard language selected by the user.
// @returns The translated tooltip string. Empty string for unknown keys.
export f_trTipTf(string key, string lang) =>
    if lang == "Türkçe"
        switch key
            "tf_title" => "Trend, Hacim, Verimlilik, Kararlılık ve Momentumu tek bir bileşik baskı okumasında sentezler"
            "tf_badge" => "Trade Finder aktif ve tüm sinyal boyutlarını işliyor"
            "tf_state" => "Trend, Hacim, Verimlilik, Kararlılık ve Momentum boyutlarından sentezlenmiş piyasa durumu"
            "tf_score" => "Bileşik baskı puanı: -100 (maksimum ayı) ile +100 (maksimum boğa) arası. 5 boyutun ağırlıklı toplamı"
            "tf_cons"  => "Boyut uzlaşısı: 5 boyuttan kaçının bileşik yönle hemfikir olduğu"
            "tf_watch" => "İzlenecek kilit faktör: Trend↔Hacim = trend ve hacim uyuşmuyor, Hacim Sapması = hacim azalıyor, Karşıtlık = güçlü karşı hareket, Verim Düşüyor = hareket kalitesi geriliyor"
            "tf_narr"  => "Mevcut durumun anlamı ve onu neyin değiştirebileceği"
            "sh_title" => "Açık mumun son çakışma dönemine karşı gerçek zamanlı kontrolü. Değerler geçicidir ve mum kapanana kadar değişebilir — asla etiket veya alarmlar için kullanılmaz."
            "sh_badge" => "Bu satır açık mumda tik tik güncellenir ve kapanışta kilitlenir (Canlı İstisna protokolü)"
            "sh_state" => "Oluşan mum, son basılan çakışma yönünü doğruluyor mu yoksa onunla savaşıyor mu?"
            "sh_score" => "Açık mumun anlık harmanlanmış bileşik puanı (geçici)"
            "sh_dom"   => "Aktif dönem yönünün baskın taraf harmanlanmış puanı (geçici)"
            => ""
    else if lang == "العربية"
        switch key
            "tf_title" => "يولّف الاتجاه والحجم والكفاءة والقناعة والزخم في قراءة ضغط مركبة واحدة"
            "tf_badge" => "Trade Finder نشط ويعالج جميع أبعاد الإشارة"
            "tf_state" => "حالة السوق المولّفة من أبعاد الاتجاه والحجم والكفاءة والقناعة والزخم"
            "tf_score" => "درجة الضغط المركبة: من -100 (أقصى هبوط) إلى +100 (أقصى صعود). مجموع مرجّح لخمسة أبعاد"
            "tf_cons"  => "إجماع الأبعاد: كم بُعداً من 5 يتفق مع الاتجاه المركب"
            "tf_watch" => "العامل الرئيسي للمراقبة: اتجاه↔حجم = الاتجاه والحجم لا يتفقان، تباين الحجم = الحجم يتراجع، معاكسة = حركة معاكسة قوية، الكفاءة تتراجع = جودة الحركة تنخفض"
            "tf_narr"  => "ماذا تعني الحالة الحالية وما الذي قد يغيّرها"
            "sh_title" => "فحص فوري للشمعة المفتوحة مقابل آخر فترة التقاء. القيم مؤقتة وقد تتغير حتى إغلاق الشمعة — لا تُستخدم أبداً للعلامات أو التنبيهات."
            "sh_badge" => "هذا الصف يُحدَّث لحظة بلحظة على الشمعة المفتوحة ويُقفل عند الإغلاق (بروتوكول الاستثناء المباشر)"
            "sh_state" => "هل الشمعة قيد التكوين تؤكد آخر اتجاه التقاء مطبوع أم تقاومه؟"
            "sh_score" => "الدرجة المركبة الممزوجة اللحظية للشمعة المفتوحة (مؤقتة)"
            "sh_dom"   => "درجة الجانب المهيمن الممزوجة لاتجاه الفترة النشطة (مؤقتة)"
            => ""
    else if lang == "Italiano"
        switch key
            "tf_title" => "Sintetizza Trend, Volume, Efficienza, Convinzione e Momentum in un'unica lettura composita della pressione"
            "tf_badge" => "Trade Finder è attivo ed elabora tutte le dimensioni del segnale"
            "tf_state" => "Stato di mercato sintetizzato dalle dimensioni Trend, Volume, Efficienza, Convinzione e Momentum"
            "tf_score" => "Punteggio di pressione composito: da -100 (massimo ribassista) a +100 (massimo rialzista). Somma ponderata di 5 dimensioni"
            "tf_cons"  => "Consenso delle dimensioni: quante delle 5 dimensioni concordano con la direzione composita"
            "tf_watch" => "Fattore chiave da osservare: Trend↔Volume = trend e volume in disaccordo, Diverg. Volume = volume in calo, Contromossa = forte movimento opposto, Efficienza in Calo = qualità del movimento in diminuzione"
            "tf_narr"  => "Cosa significa lo stato attuale e cosa potrebbe cambiarlo"
            "sh_title" => "Controllo in tempo reale della candela aperta rispetto all'ultimo episodio di confluenza. I valori sono provvisori e possono cambiare fino alla chiusura della candela — mai usati per etichette o alert."
            "sh_badge" => "Questa riga si aggiorna tick per tick sulla candela aperta e si blocca alla chiusura (protocollo Eccezione Live)"
            "sh_state" => "La candela in formazione conferma o contrasta l'ultima direzione di confluenza stampata?"
            "sh_score" => "Punteggio composito miscelato in tempo reale della candela aperta (provvisorio)"
            "sh_dom"   => "Punteggio miscelato del lato dominante della direzione dell'episodio attivo (provvisorio)"
            => ""
    else if lang == "हिन्दी"
        switch key
            "tf_title" => "ट्रेंड, वॉल्यूम, दक्षता, दृढ़ता और मोमेंटम को एक संयुक्त दबाव रीडिंग में संश्लेषित करता है"
            "tf_badge" => "Trade Finder सक्रिय है और सभी सिग्नल आयामों को संसाधित कर रहा है"
            "tf_state" => "ट्रेंड, वॉल्यूम, दक्षता, दृढ़ता और मोमेंटम आयामों से संश्लेषित बाज़ार स्थिति"
            "tf_score" => "संयुक्त दबाव स्कोर: -100 (अधिकतम मंदी) से +100 (अधिकतम तेज़ी) तक. 5 आयामों का भारित योग"
            "tf_cons"  => "आयाम सहमति: 5 में से कितने आयाम संयुक्त दिशा से सहमत हैं"
            "tf_watch" => "देखने योग्य मुख्य कारक: ट्रेंड↔वॉल्यूम = ट्रेंड और वॉल्यूम असहमत, वॉल्यूम विचलन = वॉल्यूम घट रहा, प्रतिकूल चाल = मज़बूत विपरीत चाल, दक्षता घट रही = चाल की गुणवत्ता गिर रही"
            "tf_narr"  => "मौजूदा स्थिति का अर्थ और उसे क्या बदल सकता है"
            "sh_title" => "खुली कैंडल की अंतिम संगम अवधि से वास्तविक-समय जाँच. मान अस्थायी हैं और कैंडल बंद होने तक बदल सकते हैं — लेबल या अलर्ट के लिए कभी उपयोग नहीं होते."
            "sh_badge" => "यह पंक्ति खुली कैंडल पर टिक-दर-टिक अपडेट होती है और बंद होने पर लॉक हो जाती है (लाइव अपवाद प्रोटोकॉल)"
            "sh_state" => "क्या बन रही कैंडल अंतिम मुद्रित संगम दिशा की पुष्टि कर रही है या उससे लड़ रही है?"
            "sh_score" => "खुली कैंडल का तात्कालिक मिश्रित संयुक्त स्कोर (अस्थायी)"
            "sh_dom"   => "सक्रिय अवधि की दिशा का प्रभावी-पक्ष मिश्रित स्कोर (अस्थायी)"
            => ""
    else if lang == "日本語"
        switch key
            "tf_title" => "トレンド、出来高、効率、確信、モメンタムを一つの複合圧力リーディングに統合します"
            "tf_badge" => "Trade Finderは有効で、全てのシグナル次元を処理しています"
            "tf_state" => "トレンド、出来高、効率、確信、モメンタムの各次元から統合された市場状態"
            "tf_score" => "複合圧力スコア: -100(最大弱気)から+100(最大強気)まで。5次元の加重合計"
            "tf_cons"  => "次元コンセンサス: 5次元のうち複合方向に一致する数"
            "tf_watch" => "注視すべき主要因: トレンド↔出来高 = トレンドと出来高が不一致、出来高乖離 = 出来高が減少、逆行 = 強い反対方向の動き、効率低下 = 値動きの質が低下"
            "tf_narr"  => "現在の状態の意味と、それを変えうる要因"
            "sh_title" => "確定前の足を直近のコンフルエンス局面と照合するリアルタイムチェック。値は暫定で、足が確定するまで変化します — ラベルやアラートには一切使用されません。"
            "sh_badge" => "この行は未確定足でティックごとに更新され、確定時にロックされます(ライブ例外プロトコル)"
            "sh_state" => "形成中の足は、直近に確定したコンフルエンス方向を支持していますか、それとも逆らっていますか?"
            "sh_score" => "未確定足のリアルタイム混合複合スコア(暫定)"
            "sh_dom"   => "有効な局面方向の優勢側混合スコア(暫定)"
            => ""
    else if lang == "简体中文"
        switch key
            "tf_title" => "将趋势、成交量、效率、信念与动量综合为单一复合压力读数"
            "tf_badge" => "Trade Finder已启用，正在处理所有信号维度"
            "tf_state" => "由趋势、成交量、效率、信念与动量维度综合而成的市场状态"
            "tf_score" => "复合压力评分: -100(最大看跌)至+100(最大看涨)。5个维度的加权和"
            "tf_cons"  => "维度共识: 5个维度中有多少与复合方向一致"
            "tf_watch" => "需关注的关键因素: 趋势↔成交量 = 趋势与成交量不一致，量能背离 = 成交量下降，逆向冲击 = 强劲反向行情，效率衰减 = 行情质量下降"
            "tf_narr"  => "当前状态的含义以及可能改变它的因素"
            "sh_title" => "将未收盘K线与最近一次共振阶段进行实时核对。数值为暂定，K线收盘前可能变化 — 绝不用于标签或警报。"
            "sh_badge" => "该行在未收盘K线上逐笔更新，收盘时锁定(实时例外协议)"
            "sh_state" => "正在形成的K线是在确认还是在对抗最近打印的共振方向?"
            "sh_score" => "未收盘K线的实时混合复合评分(暂定)"
            "sh_dom"   => "活跃阶段方向的主导方混合评分(暂定)"
            => ""
    else
        switch key
            "tf_title" => "Synthesizes Trend, Volume, Efficiency, Conviction and Momentum into one composite pressure read"
            "tf_badge" => "Trade Finder is active and processing all signal dimensions"
            "tf_state" => "Synthesized market state from Trend, Volume, Efficiency, Conviction, and Momentum dimensions"
            "tf_score" => "Composite pressure score: -100 (max bearish) to +100 (max bullish). Weighted sum of 5 dimensions"
            "tf_cons"  => "Dimension consensus: how many of 5 dimensions agree with the composite direction"
            "tf_watch" => "Key factor to watch: Trend↔Vol = trend and volume disagree, Vol Diverg = volume declining, Counter = strong opposing move, Eff Fading = move quality dropping"
            "tf_narr"  => "What the current state means and what could change it"
            "sh_title" => "Real-time check of the open bar against the last confluence episode. Values are provisional and can change until the bar closes — never used for labels or alerts."
            "sh_badge" => "This row updates tick by tick on the open bar and locks at the close (Live Exception protocol)"
            "sh_state" => "Is the forming bar confirming or fighting the last printed confluence direction?"
            "sh_score" => "In-flight blended composite score of the open bar (provisional)"
            "sh_dom"   => "Dominant-side blended score of the active episode direction (provisional)"
            => ""

// ══════════════════════════════════════════════════════════════════════════════════
// 8. f_trInline3 — Helper affixes for composed cell strings (4 keys)
// agree_sfx → watch cell "N/5 agree" suffix
// live_sfx  → Signal Health score " live" suffix
// bull_pfx / bear_pfx → Signal Health dominant-side "Bull +" / "Bear -" prefixes
// ══════════════════════════════════════════════════════════════════════════════════

// @function Returns translated helper affixes used inside composed dashboard strings.
// @param key Affix key identifier: agree_sfx, live_sfx, bull_pfx, bear_pfx.
// @param lang The dashboard language selected by the user.
// @returns The translated affix string. Empty string for unknown keys.
export f_trInline3(string key, string lang) =>
    if lang == "Türkçe"
        switch key
            "agree_sfx" => "/5 hemfikir"
            "live_sfx"  => " canlı"
            "bull_pfx"  => "Boğa +"
            "bear_pfx"  => "Ayı -"
            => ""
    else if lang == "العربية"
        switch key
            "agree_sfx" => "/5 متفقة"
            "live_sfx"  => " مباشر"
            "bull_pfx"  => "صعود +"
            "bear_pfx"  => "هبوط -"
            => ""
    else if lang == "Italiano"
        switch key
            "agree_sfx" => "/5 concordi"
            "live_sfx"  => " live"
            "bull_pfx"  => "Toro +"
            "bear_pfx"  => "Orso -"
            => ""
    else if lang == "हिन्दी"
        switch key
            "agree_sfx" => "/5 सहमत"
            "live_sfx"  => " लाइव"
            "bull_pfx"  => "तेज़ी +"
            "bear_pfx"  => "मंदी -"
            => ""
    else if lang == "日本語"
        switch key
            "agree_sfx" => "/5 一致"
            "live_sfx"  => " ライブ"
            "bull_pfx"  => "強気 +"
            "bear_pfx"  => "弱気 -"
            => ""
    else if lang == "简体中文"
        switch key
            "agree_sfx" => "/5 一致"
            "live_sfx"  => " 实时"
            "bull_pfx"  => "多 +"
            "bear_pfx"  => "空 -"
            => ""
    else
        switch key
            "agree_sfx" => "/5 agree"
            "live_sfx"  => " live"
            "bull_pfx"  => "Bull +"
            "bear_pfx"  => "Bear -"
            => ""

// ══════════════════════════════════════════════════════════════════════════════════
// 9. f_trTipFp3 — Native footprint display texts (4 keys)
// Covers the Native-specific strings added in ST-EP 6.3 Round 4: title tooltip,
// lock message, lock row tooltip, badge tooltip. Geometry/Intrabar strings stay
// with Lang/Lang2. Method names (Geometry, Intrabar, Native) stay untranslated by
// design — they must match the English input dropdown options.
// Input tooltips (input.*) are out of scope: Pine requires const strings there,
// so they cannot be language-routed at runtime.
// English keys pending byte-level verification against the indicator source at
// integration time — verify and sync BEFORE publishing v1.
// ══════════════════════════════════════════════════════════════════════════════════

// @function Returns Native-footprint display texts for the dashboard.
// @param key Text key: fp_title_nat, fp_lock_nat, fp_lock_tip_nat, fp_badge_nat.
// @param lang The dashboard language selected by the user.
// @returns The translated text. Empty string for unknown keys.
export f_trTipFp3(string key, string lang) =>
    if lang == "Türkçe"
        switch key
            "fp_title_nat"    => "Yerel ayak izi: TradingView'in sunucu taraflı ayak izi motorundan tik bazlı kesin alış/satış hacmi — mevcut en hassas ayrıştırma"
            "fp_lock_nat"     => "🔒 Yerel ayak izi Premium veya Ultimate plan gerektirir"
            "fp_lock_tip_nat" => "Yerel yöntem yalnızca Premium ve Ultimate planlarda sunulan request.footprint() fonksiyonunu kullanır. Planınızı yükseltin veya ayarlardan ayak izi yöntemini Geometry ya da Intrabar olarak değiştirin"
            "fp_badge_nat"    => "Kilitli: Yerel ayak izi bir Premium/Ultimate özelliğidir. Kullanılabilir bir yöntem seçildiğinde gösterge paneli otomatik olarak devam eder"
            => ""
    else if lang == "العربية"
        switch key
            "fp_title_nat"    => "البصمة الأصلية: حجم شراء/بيع دقيق قائم على التيك من محرك البصمة على خوادم TradingView — أدق تفكيك متاح"
            "fp_lock_nat"     => "🔒 البصمة الأصلية تتطلب خطة Premium أو Ultimate"
            "fp_lock_tip_nat" => "الطريقة الأصلية تستخدم request.footprint() المتاحة في خطتي Premium وUltimate فقط. رقِّ خطتك أو غيّر طريقة البصمة إلى Geometry أو Intrabar من الإعدادات"
            "fp_badge_nat"    => "مقفل: البصمة الأصلية ميزة Premium/Ultimate. تستأنف اللوحة تلقائياً عند اختيار طريقة متاحة"
            => ""
    else if lang == "Italiano"
        switch key
            "fp_title_nat"    => "Footprint nativo: volume acquisti/vendite esatto basato sui tick dal motore footprint lato server di TradingView — la scomposizione più precisa disponibile"
            "fp_lock_nat"     => "🔒 Il footprint nativo richiede un piano Premium o Ultimate"
            "fp_lock_tip_nat" => "Il metodo Nativo usa request.footprint(), disponibile solo sui piani Premium e Ultimate. Aggiorna il piano oppure imposta il metodo footprint su Geometry o Intrabar nelle impostazioni"
            "fp_badge_nat"    => "Bloccato: il footprint nativo è una funzione Premium/Ultimate. La dashboard riprende automaticamente quando si seleziona un metodo disponibile"
            => ""
    else if lang == "हिन्दी"
        switch key
            "fp_title_nat"    => "नेटिव फुटप्रिंट: TradingView के सर्वर-साइड फुटप्रिंट इंजन से टिक-आधारित सटीक खरीद/बिक्री वॉल्यूम — उपलब्ध सबसे सटीक विभाजन"
            "fp_lock_nat"     => "🔒 नेटिव फुटप्रिंट के लिए Premium या Ultimate प्लान आवश्यक है"
            "fp_lock_tip_nat" => "नेटिव विधि request.footprint() का उपयोग करती है, जो केवल Premium और Ultimate प्लान पर उपलब्ध है. अपना प्लान अपग्रेड करें या सेटिंग्स में फुटप्रिंट विधि को Geometry या Intrabar पर बदलें"
            "fp_badge_nat"    => "लॉक: नेटिव फुटप्रिंट एक Premium/Ultimate सुविधा है. उपलब्ध विधि चुनते ही डैशबोर्ड स्वतः फिर शुरू हो जाता है"
            => ""
    else if lang == "日本語"
        switch key
            "fp_title_nat"    => "ネイティブフットプリント: TradingViewのサーバーサイドフットプリントエンジンによるティックベースの正確な買い/売り出来高 — 利用可能な最も精密な分解"
            "fp_lock_nat"     => "🔒 ネイティブフットプリントにはPremiumまたはUltimateプランが必要です"
            "fp_lock_tip_nat" => "ネイティブ方式はrequest.footprint()を使用し、PremiumおよびUltimateプランでのみ利用可能です。プランをアップグレードするか、設定でフットプリント方式をGeometryまたはIntrabarに変更してください"
            "fp_badge_nat"    => "ロック中: ネイティブフットプリントはPremium/Ultimate限定機能です。利用可能な方式を選択するとダッシュボードは自動的に再開します"
            => ""
    else if lang == "简体中文"
        switch key
            "fp_title_nat"    => "原生足迹: 来自TradingView服务器端足迹引擎的基于逐笔数据的精确买/卖成交量 — 当前可用的最精确分解"
            "fp_lock_nat"     => "🔒 原生足迹需要Premium或Ultimate套餐"
            "fp_lock_tip_nat" => "原生方式使用request.footprint()，仅在Premium和Ultimate套餐中可用。请升级套餐，或在设置中将足迹方式改为Geometry或Intrabar"
            "fp_badge_nat"    => "已锁定: 原生足迹为Premium/Ultimate专属功能。选择可用方式后，仪表盘将自动恢复"
            => ""
    else
        switch key
            "fp_title_nat"    => "Native footprint: exact tick-based buy/sell volume from TradingView's server-side footprint engine — the most precise decomposition available"
            "fp_lock_nat"     => "🔒 Native footprint requires Premium or Ultimate plan"
            "fp_lock_tip_nat" => "The Native method uses request.footprint(), available on Premium and Ultimate plans only. Upgrade your plan or switch the footprint method to Geometry or Intrabar in settings"
            "fp_badge_nat"    => "Locked: Native footprint is a Premium/Ultimate feature. The dashboard resumes automatically when an available method is selected"
            => ""

// ══════════════════════════════════════════════════════════════════════════════════
// CHART-MARK TOOLTIPS (added in Round 8) — sections 10-12 follow this line.
// ══════════════════════════════════════════════════════════════════════════════════

// ══════════════════════════════════════════════════════════════════════════════════
// 10. f_cmTip — Confluence mark tooltip (chart labels)
// Replaces the inline English _mTip construction in the indicator's Confluence Mark
// Renderer. English output is byte-identical to the current inline strings.
// Pre-translated components: coupName (E01-E19 via f_trCoup) and volName (S1-S19 via
// f_trVol / f_trVol3) arrive already translated from the indicator's routers.
// Trend, conviction and momentum names resolve from their integer codes inside.
// Realization: fragment assembly — each language assigns its fragments once and one
// composition path builds the tooltip, so the line structure stays identical in all
// 7 languages and the numeric scaffolding is written exactly once.
// ══════════════════════════════════════════════════════════════════════════════════

// @function Builds the confluence mark tooltip for chart labels.
// @param kind Mark kind: +1 bullish, -1 bearish, 0 indecision (tie).
// @param sigCode Trend signal code 5..10 (gate G1 guarantees the range on mark bars).
// @param align Multi-scale alignment score, range -scN to +scN.
// @param convCode Conviction code: 1 HIGH, 2 MODERATE, 3 LOW.
// @param coupName Pre-translated efficiency coupling label (E01-E19).
// @param momCode Momentum code: 1 Accelerating, 2 Sharpening, 3 Steady, 4 Fading.
// @param volName Pre-translated volume scenario label (S1-S19).
// @param score Composite confluence score (-100..+100).
// @param consensus Trend-coherent consensus count (0..5).
// @param bullScr Jordan bull score (0..100).
// @param bearScr Jordan bear score (0..100, positive magnitude).
// @param trace Gram-matrix trace, total activation (0..5).
// @param contDir Continuation direction: 0 birth or indecision (no extra line), +1/-1 same-direction continuation.
// @param domScr Dominant score of this mark (continuation comparison).
// @param prevScr Dominant score of the previous same-direction mark.
// @param lang The dashboard language selected by the user.
// @returns The complete multi-line tooltip string.
export f_cmTip(int kind, int sigCode, int align, int convCode, string coupName, int momCode, string volName, float score, int consensus, float bullScr, float bearScr, float trace, int contDir, float domScr, float prevScr, string lang) =>
    string _head = "",   string _trN = ""
    string _cvN = "",    string _moN = ""
    string _lTrend = "", string _lAlign = ""
    string _lConv = "",  string _lEff = ""
    string _lMom = "",   string _lVol = ""
    string _lScore = "", string _lAligned = ""
    string _lBull = "",  string _lBear = ""
    string _lAct = "",   string _lDisc = ""
    string _cB = "",     string _cS = ""
    string _cStr = "",   string _cWk = ""
    string _cEq = ""
    if lang == "Türkçe"
        _head     := kind > 0 ? "BOĞA ÇAKIŞMASI — EŞİK AŞILDI" : kind < 0 ? "AYI ÇAKIŞMASI — EŞİK AŞILDI" : "KARARSIZLIK — BOĞA VE AYI EŞİKTE BERABERE"
        _trN      := sigCode == 5 ? "Güçlü Yükseliş Trendi" : sigCode == 6 ? "Güçlü Düşüş Trendi" : sigCode == 7 ? "Gizli Boğa Eğilimi" : sigCode == 8 ? "Gizli Ayı Eğilimi" : sigCode == 9 ? "Yükseliş Trendi Aktif" : "Düşüş Trendi Aktif"
        _cvN      := convCode == 1 ? "YÜKSEK" : convCode == 2 ? "ORTA" : "DÜŞÜK"
        _moN      := momCode == 1 ? "Hızlanıyor" : momCode == 2 ? "Keskinleşiyor" : momCode == 3 ? "Sabit" : "Zayıflıyor"
        _lTrend   := "Trend: "
        _lAlign   := " · Hizalama "
        _lConv    := "Kararlılık: "
        _lEff     := " · Verimlilik: "
        _lMom     := "Momentum: "
        _lVol     := "Hacim: "
        _lScore   := "Puan "
        _lAligned := "/5 hizalı · kapanmış bar verisi"
        _lBull    := "Boğa: +"
        _lBear    := " | Ayı: -"
        _lAct     := " | Aktivite: "
        _lDisc    := "Yapısal gözlem, işlem tavsiyesi değildir"
        _cB       := "Boğa devamı · "
        _cS       := "Ayı devamı · "
        _cStr     := "öncekinden daha güçlü"
        _cWk      := "öncekinden daha zayıf"
        _cEq      := "öncekiyle eşit"
    else if lang == "العربية"
        _head     := kind > 0 ? "التقاء صعودي — تم تجاوز العتبة" : kind < 0 ? "التقاء هبوطي — تم تجاوز العتبة" : "تردد — تعادل الصعود والهبوط عند العتبة"
        _trN      := sigCode == 5 ? "اتجاه صاعد قوي" : sigCode == 6 ? "اتجاه هابط قوي" : sigCode == 7 ? "ميل صعودي خفي" : sigCode == 8 ? "ميل هبوطي خفي" : sigCode == 9 ? "اتجاه صاعد نشط" : "اتجاه هابط نشط"
        _cvN      := convCode == 1 ? "مرتفعة" : convCode == 2 ? "متوسطة" : "منخفضة"
        _moN      := momCode == 1 ? "يتسارع" : momCode == 2 ? "يشتد" : momCode == 3 ? "مستقر" : "يتلاشى"
        _lTrend   := "الاتجاه: "
        _lAlign   := " · المحاذاة "
        _lConv    := "القناعة: "
        _lEff     := " · الكفاءة: "
        _lMom     := "الزخم: "
        _lVol     := "الحجم: "
        _lScore   := "الدرجة "
        _lAligned := "/5 متوافقة · بيانات شموع مغلقة"
        _lBull    := "صعود: +"
        _lBear    := " | هبوط: -"
        _lAct     := " | النشاط: "
        _lDisc    := "ملاحظة هيكلية، وليست نصيحة تداول"
        _cB       := "استمرار صعودي · "
        _cS       := "استمرار هبوطي · "
        _cStr     := "أقوى من السابق"
        _cWk      := "أضعف من السابق"
        _cEq      := "مساوٍ للسابق"
    else if lang == "Italiano"
        _head     := kind > 0 ? "CONFLUENZA RIALZISTA — SOGLIA SUPERATA" : kind < 0 ? "CONFLUENZA RIBASSISTA — SOGLIA SUPERATA" : "INDECISIONE — TORO E ORSO PARI ALLA SOGLIA"
        _trN      := sigCode == 5 ? "Forte Uptrend" : sigCode == 6 ? "Forte Downtrend" : sigCode == 7 ? "Bias Rialzista Nascosto" : sigCode == 8 ? "Bias Ribassista Nascosto" : sigCode == 9 ? "Uptrend Attivo" : "Downtrend Attivo"
        _cvN      := convCode == 1 ? "ALTA" : convCode == 2 ? "MODERATA" : "BASSA"
        _moN      := momCode == 1 ? "In Accelerazione" : momCode == 2 ? "In Affinamento" : momCode == 3 ? "Stabile" : "In Calo"
        _lTrend   := "Trend: "
        _lAlign   := " · Allineamento "
        _lConv    := "Convinzione: "
        _lEff     := " · Efficienza: "
        _lMom     := "Momentum: "
        _lVol     := "Volume: "
        _lScore   := "Punteggio "
        _lAligned := "/5 allineate · dati a barre chiuse"
        _lBull    := "Toro: +"
        _lBear    := " | Orso: -"
        _lAct     := " | Attività: "
        _lDisc    := "Osservazione strutturale, non un consiglio di trading"
        _cB       := "Continuazione rialzista · "
        _cS       := "Continuazione ribassista · "
        _cStr     := "più forte della precedente"
        _cWk      := "più debole della precedente"
        _cEq      := "pari alla precedente"
    else if lang == "हिन्दी"
        _head     := kind > 0 ? "तेज़ी संगम — सीमा पार हुई" : kind < 0 ? "मंदी संगम — सीमा पार हुई" : "अनिर्णय — सीमा पर तेज़ी और मंदी बराबर"
        _trN      := sigCode == 5 ? "मज़बूत अपट्रेंड" : sigCode == 6 ? "मज़बूत डाउनट्रेंड" : sigCode == 7 ? "छिपा तेज़ी झुकाव" : sigCode == 8 ? "छिपा मंदी झुकाव" : sigCode == 9 ? "सक्रिय अपट्रेंड" : "सक्रिय डाउनट्रेंड"
        _cvN      := convCode == 1 ? "उच्च" : convCode == 2 ? "मध्यम" : "निम्न"
        _moN      := momCode == 1 ? "तेज़ हो रहा" : momCode == 2 ? "निखर रहा" : momCode == 3 ? "स्थिर" : "घट रहा"
        _lTrend   := "ट्रेंड: "
        _lAlign   := " · संरेखण "
        _lConv    := "दृढ़ता: "
        _lEff     := " · दक्षता: "
        _lMom     := "मोमेंटम: "
        _lVol     := "वॉल्यूम: "
        _lScore   := "स्कोर "
        _lAligned := "/5 संरेखित · बंद बार डेटा"
        _lBull    := "तेज़ी: +"
        _lBear    := " | मंदी: -"
        _lAct     := " | सक्रियता: "
        _lDisc    := "संरचनात्मक अवलोकन, ट्रेड सलाह नहीं"
        _cB       := "तेज़ी निरंतरता · "
        _cS       := "मंदी निरंतरता · "
        _cStr     := "पिछले से मज़बूत"
        _cWk      := "पिछले से कमज़ोर"
        _cEq      := "पिछले के बराबर"
    else if lang == "日本語"
        _head     := kind > 0 ? "強気コンフルエンス — しきい値を突破" : kind < 0 ? "弱気コンフルエンス — しきい値を突破" : "迷い — しきい値で強気と弱気が拮抗"
        _trN      := sigCode == 5 ? "強い上昇トレンド" : sigCode == 6 ? "強い下降トレンド" : sigCode == 7 ? "隠れた強気バイアス" : sigCode == 8 ? "隠れた弱気バイアス" : sigCode == 9 ? "上昇トレンド継続中" : "下降トレンド継続中"
        _cvN      := convCode == 1 ? "高" : convCode == 2 ? "中" : "低"
        _moN      := momCode == 1 ? "加速中" : momCode == 2 ? "改善中" : momCode == 3 ? "安定" : "低下中"
        _lTrend   := "トレンド: "
        _lAlign   := " · アライメント "
        _lConv    := "確信: "
        _lEff     := " · 効率: "
        _lMom     := "モメンタム: "
        _lVol     := "出来高: "
        _lScore   := "スコア "
        _lAligned := "/5 一致 · 確定足データ"
        _lBull    := "強気: +"
        _lBear    := " | 弱気: -"
        _lAct     := " | 活性度: "
        _lDisc    := "構造的観察であり、取引助言ではありません"
        _cB       := "強気の継続 · "
        _cS       := "弱気の継続 · "
        _cStr     := "前回より強い"
        _cWk      := "前回より弱い"
        _cEq      := "前回と同等"
    else if lang == "简体中文"
        _head     := kind > 0 ? "看涨共振 — 已突破阈值" : kind < 0 ? "看跌共振 — 已突破阈值" : "犹豫 — 多空在阈值处持平"
        _trN      := sigCode == 5 ? "强劲上升趋势" : sigCode == 6 ? "强劲下降趋势" : sigCode == 7 ? "隐性看涨倾向" : sigCode == 8 ? "隐性看跌倾向" : sigCode == 9 ? "上升趋势活跃" : "下降趋势活跃"
        _cvN      := convCode == 1 ? "高" : convCode == 2 ? "中" : "低"
        _moN      := momCode == 1 ? "加速中" : momCode == 2 ? "改善中" : momCode == 3 ? "平稳" : "衰减中"
        _lTrend   := "趋势: "
        _lAlign   := " · 对齐 "
        _lConv    := "信念: "
        _lEff     := " · 效率: "
        _lMom     := "动量: "
        _lVol     := "成交量: "
        _lScore   := "评分 "
        _lAligned := "/5 一致 · 已收盘数据"
        _lBull    := "多: +"
        _lBear    := " | 空: -"
        _lAct     := " | 活跃度: "
        _lDisc    := "结构性观察，并非交易建议"
        _cB       := "看涨延续 · "
        _cS       := "看跌延续 · "
        _cStr     := "强于前一次"
        _cWk      := "弱于前一次"
        _cEq      := "与前一次持平"
    else
        _head     := kind > 0 ? "BULLISH CONFLUENCE — THRESHOLD CROSSED" : kind < 0 ? "BEARISH CONFLUENCE — THRESHOLD CROSSED" : "INDECISION — BULL AND BEAR TIED AT THRESHOLD"
        _trN      := sigCode == 5 ? "Strong Uptrend" : sigCode == 6 ? "Strong Downtrend" : sigCode == 7 ? "Hidden Bullish Bias" : sigCode == 8 ? "Hidden Bearish Bias" : sigCode == 9 ? "Uptrend Active" : "Downtrend Active"
        _cvN      := convCode == 1 ? "HIGH" : convCode == 2 ? "MODERATE" : "LOW"
        _moN      := momCode == 1 ? "Accelerating" : momCode == 2 ? "Sharpening" : momCode == 3 ? "Steady" : "Fading"
        _lTrend   := "Trend: "
        _lAlign   := " · Align "
        _lConv    := "Conviction: "
        _lEff     := " · Efficiency: "
        _lMom     := "Momentum: "
        _lVol     := "Volume: "
        _lScore   := "Score "
        _lAligned := "/5 aligned · closed-bar data"
        _lBull    := "Bull: +"
        _lBear    := " | Bear: -"
        _lAct     := " | Activity: "
        _lDisc    := "Structural observation, not trade advice"
        _cB       := "Bullish continuation · "
        _cS       := "Bearish continuation · "
        _cStr     := "stronger than previous"
        _cWk      := "weaker than previous"
        _cEq      := "equal to previous"
    string _tip = _head
         + "\n" + _lTrend + _trN + _lAlign + (align >= 0 ? "+" : "") + str.tostring(align)
         + "\n" + _lConv + _cvN + _lEff + coupName
         + "\n" + _lMom + _moN
         + "\n" + _lVol + volName
         + "\n" + _lScore + (score >= 0 ? "+" : "") + str.tostring(score, "#") + " · " + str.tostring(consensus) + _lAligned
         + "\n" + _lBull + str.tostring(bullScr, "#") + _lBear + str.tostring(bearScr, "#") + _lAct + str.tostring(trace, "#.#") + "/5"
         + "\n" + _lDisc
    if contDir != 0
        _tip += "\n" + (contDir > 0 ? _cB : _cS) + (domScr > prevScr ? _cStr : domScr < prevScr ? _cWk : _cEq) + " (" + str.tostring(domScr, "#") + " vs " + str.tostring(prevScr, "#") + ")"
    _tip

// ══════════════════════════════════════════════════════════════════════════════════
// 11. f_rvTip — Reversal (REV) label tooltip (chart labels)
// Replaces the inline English _rvTip construction in the REVERSAL & TREND STATE
// ENGINE event renderer. English output is byte-identical to the inline strings.
// The REV label text itself stays untranslated (compact chart code, same policy as
// S12/S13). Class 2 resolves by direction: Accumulation (bull) / Distribution (bear).
// ══════════════════════════════════════════════════════════════════════════════════

// @function Builds the reversal (REV) label tooltip for chart labels.
// @param bull True for a bullish reversal, false for a bearish one.
// @param rvClass Classifier class: 1 Climactic, 2 Accumulation (bull) / Distribution (bear), 3 Exhaustion, 4 Rotation.
// @param prevPeak Peak |score| of the finished episode (depth feature).
// @param vel First-order score difference on the reversal bar (crossing velocity).
// @param score Composite pressure score on the reversal bar.
// @param lang The dashboard language selected by the user.
// @returns The complete multi-line tooltip string.
export f_rvTip(bool bull, int rvClass, float prevPeak, float vel, float score, string lang) =>
    string _hB = "",     string _hS = ""
    string _nm = "",     string _lDepth = ""
    string _lVel = "",   string _lNow = ""
    string _lDisc = ""
    if lang == "Türkçe"
        _hB     := "BOĞA DÖNÜŞÜ — "
        _hS     := "AYI DÖNÜŞÜ — "
        _nm     := rvClass == 1 ? "KLİMAKS" : rvClass == 2 ? (bull ? "TOPLAMA" : "DAĞITIM") : rvClass == 3 ? "TÜKENME" : "ROTASYON"
        _lDepth := "Önceki dönem derinliği: "
        _lVel   := "Geçiş hızı: "
        _lNow   := "Şu anki puan: "
        _lDisc  := "Yapısal gözlem, işlem tavsiyesi değildir"
    else if lang == "العربية"
        _hB     := "انعكاس صعودي — "
        _hS     := "انعكاس هبوطي — "
        _nm     := rvClass == 1 ? "ذروة" : rvClass == 2 ? (bull ? "تجميع" : "تصريف") : rvClass == 3 ? "إرهاق" : "تدوير"
        _lDepth := "عمق الفترة السابقة: "
        _lVel   := "سرعة العبور: "
        _lNow   := "الدرجة الآن: "
        _lDisc  := "ملاحظة هيكلية، وليست نصيحة تداول"
    else if lang == "Italiano"
        _hB     := "INVERSIONE RIALZISTA — "
        _hS     := "INVERSIONE RIBASSISTA — "
        _nm     := rvClass == 1 ? "CLIMAX" : rvClass == 2 ? (bull ? "ACCUMULAZIONE" : "DISTRIBUZIONE") : rvClass == 3 ? "ESAURIMENTO" : "ROTAZIONE"
        _lDepth := "Profondità dell'episodio precedente: "
        _lVel   := "Velocità di attraversamento: "
        _lNow   := "Punteggio attuale: "
        _lDisc  := "Osservazione strutturale, non un consiglio di trading"
    else if lang == "हिन्दी"
        _hB     := "तेज़ी पलटाव — "
        _hS     := "मंदी पलटाव — "
        _nm     := rvClass == 1 ? "क्लाइमेक्स" : rvClass == 2 ? (bull ? "संचय" : "वितरण") : rvClass == 3 ? "थकावट" : "रोटेशन"
        _lDepth := "पिछली अवधि की गहराई: "
        _lVel   := "क्रॉसिंग वेग: "
        _lNow   := "वर्तमान स्कोर: "
        _lDisc  := "संरचनात्मक अवलोकन, ट्रेड सलाह नहीं"
    else if lang == "日本語"
        _hB     := "強気反転 — "
        _hS     := "弱気反転 — "
        _nm     := rvClass == 1 ? "クライマックス" : rvClass == 2 ? (bull ? "買い集め" : "売り抜け") : rvClass == 3 ? "枯渇" : "ローテーション"
        _lDepth := "前局面の深さ: "
        _lVel   := "クロス速度: "
        _lNow   := "現在のスコア: "
        _lDisc  := "構造的観察であり、取引助言ではありません"
    else if lang == "简体中文"
        _hB     := "看涨反转 — "
        _hS     := "看跌反转 — "
        _nm     := rvClass == 1 ? "高潮" : rvClass == 2 ? (bull ? "吸筹" : "派发") : rvClass == 3 ? "衰竭" : "轮动"
        _lDepth := "前一阶段深度: "
        _lVel   := "穿越速度: "
        _lNow   := "当前评分: "
        _lDisc  := "结构性观察，并非交易建议"
    else
        _hB     := "BULLISH REVERSAL — "
        _hS     := "BEARISH REVERSAL — "
        _nm     := rvClass == 1 ? "CLIMACTIC" : rvClass == 2 ? (bull ? "ACCUMULATION" : "DISTRIBUTION") : rvClass == 3 ? "EXHAUSTION" : "ROTATION"
        _lDepth := "Prior episode depth: "
        _lVel   := "Cross velocity: "
        _lNow   := "Score now: "
        _lDisc  := "Structural observation, not trade advice"
    (bull ? _hB : _hS) + _nm
         + "\n" + _lDepth + str.tostring(prevPeak, "#")
         + "\n" + _lVel + (vel >= 0 ? "+" : "") + str.tostring(vel, "#.#")
         + "\n" + _lNow + (score >= 0 ? "+" : "") + str.tostring(score, "#")
         + "\n" + _lDisc

// ══════════════════════════════════════════════════════════════════════════════════
// 12. f_rtTip — Trend confirmation (TREND) label tooltip (chart labels)
// Replaces the inline English _rtTip construction in the REVERSAL & TREND STATE
// ENGINE event renderer. English output is byte-identical to the inline strings.
// The ▲ TREND / ▼ TREND label text itself stays untranslated (compact chart code).
// ══════════════════════════════════════════════════════════════════════════════════

// @function Builds the trend confirmation (TREND) label tooltip for chart labels.
// @param bull True for a bullish trend confirmation, false for a bearish one.
// @param score Composite pressure score on the confirmation bar.
// @param trendTh Trend Confirmation Level (the user's RS_TREND_TH input).
// @param bars Bars elapsed since the reversal that opened the episode.
// @param lang The dashboard language selected by the user.
// @returns The complete multi-line tooltip string.
export f_rtTip(bool bull, float score, float trendTh, int bars, string lang) =>
    string _hB = "",      string _hS = ""
    string _lReach = "",  string _lTh = ""
    string _lBars = "",   string _lActive = ""
    string _lDisc = ""
    if lang == "Türkçe"
        _hB      := "BOĞA TRENDİ TESPİT EDİLDİ"
        _hS      := "AYI TRENDİ TESPİT EDİLDİ"
        _lReach  := "Ulaşılan puan: "
        _lTh     := " (eşik "
        _lBars   := "Dönüşten bu yana bar sayısı: "
        _lActive := "Bir sonraki karşı dönüşe kadar aktif"
        _lDisc   := "Yapısal gözlem, işlem tavsiyesi değildir"
    else if lang == "العربية"
        _hB      := "تم رصد اتجاه صعودي"
        _hS      := "تم رصد اتجاه هبوطي"
        _lReach  := "وصلت الدرجة إلى "
        _lTh     := " (العتبة "
        _lBars   := "عدد الشموع منذ الانعكاس: "
        _lActive := "نشط حتى الانعكاس المعاكس التالي"
        _lDisc   := "ملاحظة هيكلية، وليست نصيحة تداول"
    else if lang == "Italiano"
        _hB      := "TREND RIALZISTA RILEVATO"
        _hS      := "TREND RIBASSISTA RILEVATO"
        _lReach  := "Punteggio raggiunto: "
        _lTh     := " (soglia "
        _lBars   := "Barre dall'inversione: "
        _lActive := "Attivo fino alla prossima inversione opposta"
        _lDisc   := "Osservazione strutturale, non un consiglio di trading"
    else if lang == "हिन्दी"
        _hB      := "तेज़ी ट्रेंड का पता चला"
        _hS      := "मंदी ट्रेंड का पता चला"
        _lReach  := "स्कोर पहुँचा: "
        _lTh     := " (सीमा "
        _lBars   := "पलटाव के बाद बार: "
        _lActive := "अगले विपरीत पलटाव तक सक्रिय"
        _lDisc   := "संरचनात्मक अवलोकन, ट्रेड सलाह नहीं"
    else if lang == "日本語"
        _hB      := "強気トレンドを検出"
        _hS      := "弱気トレンドを検出"
        _lReach  := "スコア到達: "
        _lTh     := " (しきい値 "
        _lBars   := "反転からのバー数: "
        _lActive := "次の反対方向の反転まで有効"
        _lDisc   := "構造的観察であり、取引助言ではありません"
    else if lang == "简体中文"
        _hB      := "检测到看涨趋势"
        _hS      := "检测到看跌趋势"
        _lReach  := "评分达到 "
        _lTh     := " (阈值 "
        _lBars   := "反转以来K线数: "
        _lActive := "在下一次反向反转前保持有效"
        _lDisc   := "结构性观察，并非交易建议"
    else
        _hB      := "BULLISH TREND DETECTED"
        _hS      := "BEARISH TREND DETECTED"
        _lReach  := "Score reached "
        _lTh     := " (threshold "
        _lBars   := "Bars since reversal: "
        _lActive := "Active until the next opposite reversal"
        _lDisc   := "Structural observation, not trade advice"
    (bull ? _hB : _hS)
         + "\n" + _lReach + (score >= 0 ? "+" : "") + str.tostring(score, "#") + _lTh + str.tostring(trendTh, "#") + ")"
         + "\n" + _lBars + str.tostring(bars)
         + "\n" + _lActive
         + "\n" + _lDisc

// ══════════════════════════════════════════════════════════════════════════════════
// END OF LIBRARY — Round 8 complete: 12 functions, all 7 languages (EN, TR, AR, IT,
// HI, JA, ZH). Chart-mark tooltips added (f_cmTip, f_rvTip, f_rtTip).
// Open gate: f_trTipFp3 English-key decision (Native texts) — pending Ata's call.
// Publish as v2 — the indicator will import ata_sabanci/SNIFOFF_51_Lang3/2.
// ══════════════════════════════════════════════════════════════════════════════════